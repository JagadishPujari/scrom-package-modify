# Scrom package content modify on fly
## Setup project
1. In cloned project root directory 
```
npm install
```
2. Once done, place scrom package files in `/scrom_packages` in this folder.
3. Start server 
  ```
    node server.js
